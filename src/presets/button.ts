import { Preset } from "unocss";

const preset: Preset = {
    name: "blech-ui/button",
    shortcuts: {
        btn: "px-2 py-1 bg-green rounded text-green-content",
    },
};

export default preset;
